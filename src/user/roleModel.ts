/**
 * User role definition.
 */
export interface RoleModel {
    /**
     * Role key, e.g. "anonymous";
     */
    key: string;

    /**
     * Role name, e.g. "Anonymous";
     */
    name: string;
}