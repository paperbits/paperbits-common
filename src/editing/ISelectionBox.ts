export interface ISelectionBox { //TODO: Leave just Box
    top: number;
    left: number;
    width: number;
    height: number;
}

export interface Box {
    top: number;
    left: number;
    width: number;
    height: number;
}
