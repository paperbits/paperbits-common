export interface LocaleModel {
    /**
     * Own key.
     */
    key: string;

    /**
     * Locale code, e.g. "en-us".
     */
    code: string;

    /**
     * Locale display name, e.g. English (US). 
     */
    displayName: string;
}